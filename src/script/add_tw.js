export const tooogle_addtw = (status) =>{
    var add_tw=document.getElementsByClassName("hp-addtw")[0];
    if (status===0)
        add_tw.style.display="none";
    else
        add_tw.style.display="block";
        
}
